{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "title": "Payments Created",
      "type": "stat",
      "targets": [{ "expr": "sum(rate(payment_transactions_created_total[5m]))" }],
      "fieldConfig": { "defaults": { "thresholds": { "steps": [{ "color": "green", "value": null }] } } }
    },
    {
      "title": "Orders Processed",
      "type": "stat",
      "targets": [{ "expr": "sum(rate(order_processed_total[5m]))" }],
      "fieldConfig": { "defaults": { "thresholds": { "steps": [{ "color": "blue", "value": null }] } } }
    },
    {
      "title": "Ledger Found",
      "type": "stat",
      "targets": [{ "expr": "sum(rate(ledger_transactions_found_total[5m]))" }],
      "fieldConfig": { "defaults": { "thresholds": { "steps": [{ "color": "green", "value": null }] } } }
    },
    {
      "title": "Ledger NOT FOUND",
      "type": "stat",
      "targets": [{ "expr": "sum(rate(ledger_transactions_not_found_total[5m]))" }],
      "fieldConfig": { "defaults": { "thresholds": { "steps": [{ "color": "red", "value": 0 }] } } }
    }
  ],
  "schemaVersion": 38,
  "style": "dark",
  "tags": ["demo"],
  "templating": { "list": [] },
  "time": { "from": "now-1h", "to": "now" },
  "title": "Transaction Flow",
  "uid": "transaction-flow",
  "version": 1
}
